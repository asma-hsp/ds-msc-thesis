%!TEX root = ../dissertation.tex

% =====================================================================
%  CHAPTER 5 — EXPERIMENTAL RESULTS   (DRAFT v2: five instructions, avg-5,
%  full i-CIR throughout, SigLIP2 post-processing ladder)
% ---------------------------------------------------------------------
%  Numbers sourced from thesis_cir/runs/caption_backbone_instancepool/
%  (per-backbone summary.csv) and .../siglip2_ladder/summary_ladder.csv.
%  All headline numbers are macro-mAP (%) on the FULL i-CIR dataset
%  (1,883 queries / 752,092 DB), instance-pool protocol.
%  HIGHLIGHT markers = figures/examples to insert.
% =====================================================================

\chapter{Experimental Results}\label{chapt:results}

This chapter reports and discusses the experiments that evaluate the proposed
pipeline. Section~\ref{sect:res_setup} describes the experimental setup.
Section~\ref{sect:res_main} reports the main result -- the \textsc{Basic} duplet
versus our triplet -- across all four backbones on the full i-CIR benchmark.
Section~\ref{sect:res_components} then dissects the pipeline: it isolates the
contribution of each component on a cumulative ladder
(Section~\ref{subsect:res_ladder}), studies the effect of feature centering and
the remaining \textsc{Basic} post-processing
(Section~\ref{sect:res_centering}), compares the five caption-generation
instructions (Section~\ref{subsect:res_ablation_instr}), and quantifies the
benefit of embedding-space averaging
(Section~\ref{subsect:res_aug}). Section~\ref{sect:res_backbones} analyses the
backbones and establishes the best result attainable.
Section~\ref{sect:res_qual} presents a qualitative and failure analysis, and
Section~\ref{sect:res_summary} summarises the findings.

% =====================================================================
\section{Experimental Setup}\label{sect:res_setup}
% =====================================================================

\paragraph{Data and protocol.} All results are reported on the full i-CIR
benchmark~\cite{psomas2025icir}: $1{,}883$ queries ranked against
$N = 752{,}092$ database images, under the instance-pool retrieval protocol of
Section~\ref{sect:method_eval}. Every number in this chapter is computed at full
scale.

\paragraph{Metrics.} Unless stated otherwise, all numbers are macro-mAP
(\textsc{mmAP}, \%), the primary metric of Section~\ref{sect:method_eval}
(Equation~\eqref{eq:macro_map}); micro-mAP (\textsc{mAP}) is reported alongside
where informative. Recall@$k$ (R@$k$, the fraction of queries with at least one
correct item in the top $k$) is additionally reported in the backbone analysis of
Section~\ref{sect:res_backbones}.

\paragraph{Configurations.} We compare the \textsc{Basic} duplet
(image$\times$text, Equation~\eqref{eq:duplet}) against \emph{Ours}, the
clamped triplet image$\times$text$\times$caption of
Equation~\eqref{eq:triplet}. The caption signal uses the \texttt{avg-5} setting
(Equation~\eqref{eq:avg5}) by default; ``Ours~[best instr.]'' denotes the
triplet using the single best-performing instruction of
Table~\ref{tab:instructions} instead of \texttt{avg-5}. Feature centering and the
\textsc{Basic} post-processing of Section~\ref{sect:method_basic} are applied as
a cumulative ladder, analysed in Section~\ref{sect:res_components}.

% =====================================================================
\section{Main Results: \textsc{Basic} Duplet vs.\ Triplet}\label{sect:res_main}
% =====================================================================

\paragraph{Comparison with the state of the art.}
Table~\ref{tab:res_leaderboard} compares the proposed method against the
literature on the full i-CIR benchmark, following the evaluation protocol of the
i-CIR paper~\cite{psomas2025icir}: macro-mAP on $1{,}883$ queries against the
$752{,}092$-image instance pool, CLIP ViT-L/14 backbone (the default choice in
the original benchmark). State-of-the-art results (marked $\dagger\dagger$) are
taken directly from Table~1 of \citeauthor{psomas2025icir}~\citeyear{psomas2025icir}.

Two ablation entries are worth noting.
\textbf{Caption alone} ($16.16$) outperforms \textbf{Caption $\times$ Image}
($14.04$), suggesting that multiplying with the image embedding can
\emph{hurt} when the backbone is weakly aligned: the image signal drags the
composed query towards unmodified instance images, diluting the textual
modification. Yet caption alone already rivals the full \textbf{Text $\times$
Image} duplet ($17.95$), showing that a self-contained target description is a
surprisingly effective query on its own.

Our best configuration, \textbf{Ours (\texttt{avg-5})}, achieves $35.76$ macro-mAP,
outperforming the previous best, CoVR-2, by $+7.3$ points and exceeding the
strongest reproduction of \textsc{Basic} (\textsc{Basic}$^\dagger$, $32.48$) by
$+3.3$ points. A note on reproduction: our \textsc{Basic}$^\dagger$ reproduction
uses a newer extraction environment (\texttt{open\_clip}~3.3.0, \texttt{Pillow}~12,
\texttt{numpy}~2.x) versus the paper's original, producing features that differ
slightly from the paper's reported $34.35$; the gap is fully explained by this
environment mismatch, not a code error, and the \emph{relative} improvement of
Ours over \textsc{Basic} is unaffected.

% SOURCE: docs/main_leaderboard_clipl.csv  (borrowed = True rows from i-CIR Table 1)
\begin{table}[h!]
\centering
\begin{tabular}{l c}
\toprule
Method & mmAP \\ \midrule
\multicolumn{2}{l}{\textit{Ablations}} \\
\quad Text ($\st$ only)                            &  3.26 \\
\quad Image ($\sv$ only)                           &  2.75 \\
\quad Text $+$ Image (sum)                         &  8.89 \\
\quad Caption ($\sc$ only)                         & 16.16 \\
\quad Caption $\times$ Image                       & 14.04 \\
\quad Text $\times$ Image (\textsc{Basic} duplet)  & 17.95 \\ \midrule
\multicolumn{2}{l}{\textit{Literature}$^{\dagger\dagger}$} \\
\quad WeiCom~\cite{psomas2025icir}                 & 18.03 \\
\quad Pic2Word~\cite{saito2023pic2word}            & 19.36 \\
\quad CompoDiff~\cite{psomas2025icir}              &  9.63 \\
\quad CIReVL~\cite{karthik2024cirevl}              & 18.66 \\
\quad SEARLE~\cite{baldrati2023circo}              & 19.90 \\
\quad MCL~\cite{psomas2025icir}                    & 19.89 \\
\quad MagicLens~\cite{psomas2025icir}              & 27.35 \\
\quad CoVR-2~\cite{psomas2025icir}                 & 28.50 \\
\quad FreeDom~\cite{psomas2025icir}                & 17.24 \\ \midrule
\multicolumn{2}{l}{\textit{\textsc{Basic} reproductions}} \\
\quad \textsc{Basic}                               & 30.28 \\
\quad \textsc{Basic}$^\dagger$                     & 32.48 \\ \midrule
\multicolumn{2}{l}{\textit{Ours}} \\
\quad Ours (caption \textsc{J})                    & 34.06 \\
\quad \textbf{Ours (\texttt{avg-5})}               & \textbf{35.76} \\
\bottomrule
\end{tabular}
\caption[Macro-mAP on the full i-CIR benchmark (CLIP-L backbone).]{Macro-mAP
(\%) on the full i-CIR dataset ($1{,}883$ queries / $752{,}092$ DB),
CLIP ViT-L/14 backbone. $^{\dagger\dagger}$Numbers taken from Table~1 of
\citeauthor{psomas2025icir}~\citeyear{psomas2025icir}; all other rows are our
evaluation. \textsc{Basic}$^\dagger$ applies the full post-processing ladder
(center $\to$ normSim $\to$ Harris $\to$ contextualisation); \textsc{Basic}
omits the CLIP-specific projection and query expansion.\label{tab:res_leaderboard}}
\end{table}

\paragraph{Gains across backbones.}
Table~\ref{tab:res_main} shows the impact of adding the caption across all four
backbones. The gains are largest where the duplet baseline is weakest:
SigLIP-L improves by $+22.2$ macro-mAP and SigLIP2 by $+18.0$, while the
already-strong CLIP-H and CLIP-L improve by $+6.0$ and $+7.5$ respectively.
The pattern is consistent with the caption's role: when the backbone's text
encoder is poorly aligned with the visual space, the modification text $\st$
carries little signal; the self-contained target caption $\sc$ is more
image-like and therefore benefits such backbones the most. On every backbone,
\texttt{avg-5} further outperforms the best single instruction (Section~\ref{subsect:res_aug}).
The best configuration -- SigLIP2 with \texttt{avg-5} -- reaches $56.09$
macro-mAP at the plain product rung, before post-processing.

% SOURCE: runs/caption_backbone_instancepool/<bk>/summary.csv
%   BASIC = BASE_modtext|product ; Ours[avg-5] = AUG_AVG5|triple ;
%   Ours[best] = max single INSTR_*|triple per backbone
\begin{table}[h!]
\centering
\begin{tabular}{l c c c c}
\toprule
Backbone & \textsc{Basic} & Ours [\texttt{avg-5}] & Ours [best instr.] & $\Delta$ \\ \midrule
CLIP ViT-L/14       & 17.95 & 25.41          & 23.83 & $+7.46$ \\
CLIP ViT-H/14       & 41.10 & 47.05          & 45.63 & $+5.95$ \\
SigLIP ViT-L/16-256 & 21.10 & 43.33          & 41.88 & $+22.23$ \\
SigLIP2 g/16-384    & 38.11 & \textbf{56.09} & 53.91 & $+17.98$ \\
\bottomrule
\end{tabular}
\caption[\textsc{Basic} duplet vs.\ triplet across backbones.]{Macro-mAP (\%)
on the full i-CIR dataset. \textsc{Basic} is the image$\times$text duplet
(Equation~\eqref{eq:duplet}); Ours adds the generated caption
(Equation~\eqref{eq:triplet}). $\Delta$ is the gain of \texttt{avg-5} over
\textsc{Basic}.\label{tab:res_main}}
\end{table}

% =====================================================================
\section{Component Analysis}\label{sect:res_components}
% =====================================================================

This section isolates the contribution of each component of the pipeline. We
work on the best backbone, SigLIP2, where the full \textsc{Basic} post-processing
stack of Section~\ref{sect:method_basic} can be layered on top of the triplet.

\subsection{Building the Pipeline: a Component Ladder}\label{subsect:res_ladder}

Table~\ref{tab:res_ladder} builds the pipeline up one component at a time, as a
cumulative ladder, for three configurations that share the \emph{exact same}
post-processing so the caption is the only isolated variable: \textsc{Basic}
(no caption), Ours~[\texttt{avg-5}], and Ours~[\textsc{H}] (the best single
instruction for SigLIP2). The first row is the plain product score; each
subsequent row adds one preprocessing step -- feature centering, similarity
min-normalisation, the Harris-style fusion penalty, query
contextualisation, the contrastive projection, and query
expansion.\footnote{SigLIP2 has no synthetic calibration set of the kind used to
fit the original \textsc{Basic} statistics for CLIP; the min-normalisation
constants are therefore estimated empirically from a database sample, and
centering uses the global database mean (Section~\ref{subsect:method_centering}).}

The single largest jump comes from the \textbf{caption}. At the product level,
adding the \texttt{avg-5} caption raises macro-mAP from $38.12$ (\textsc{Basic})
to $56.09$ -- a $+17.97$ gain that is larger than the entire post-processing
stack contributes on top of it. This is the core empirical message of the thesis:
\emph{a single generated target caption injects the textual signal more
effectively than the multi-step \textsc{Basic} preprocessing it is designed to
replace}, and it does so as a simple additive branch. The two ideas are
nonetheless complementary -- centering and contextualisation lift Ours a further
$+5.9$ to the ceiling of $61.98$ -- but the caption is unambiguously the dominant
component.

% SOURCE: runs/caption_backbone_instancepool/siglip2_ladder/summary_ladder.csv
\begin{table}[h!]
\centering
\begin{tabular}{l c c c}
\toprule
Cumulative step & \textsc{Basic} & Ours [\texttt{avg-5}] & Ours [\textsc{H}] \\ \midrule
product (no post-processing)   & 38.12 & 56.09 & 53.91 \\
\quad + centering              & 49.96 & 55.51 & 54.39 \\
\quad + min-normalisation      & 45.49 & 53.70 & 51.40 \\
\quad + Harris fusion          & 47.64 & 55.87 & 54.04 \\
\quad + contextualisation      & 57.69 & \textbf{61.98} & 60.31 \\
\quad + projection             & 52.67 & 59.02 & 57.17 \\
\quad + query expansion        & 49.56 & 57.29 & 55.35 \\
\bottomrule
\end{tabular}
\caption[Component ladder on SigLIP2 (full i-CIR).]{Macro-mAP (\%) on the full
i-CIR dataset, building the pipeline one component at a time on SigLIP2. All
three columns share the same post-processing; only the caption term differs.
The best result of the thesis ($61.98$) is Ours~[\texttt{avg-5}] at the
contextualisation rung.\label{tab:res_ladder}}
\end{table}

\subsection{Feature Centering and Post-Processing}\label{sect:res_centering}

Reading Table~\ref{tab:res_ladder} down the rungs reveals which parts of the
\textsc{Basic} stack actually help once the caption is present.
\textbf{Centering} is the second most important component after the caption: for
the \textsc{Basic} duplet it alone lifts macro-mAP from $38.12$ to $49.96$
($+11.8$), confirming a strong embedding-space bias in SigLIP2 that subtracting
the mean removes. \textbf{Contextualisation} is the other clear winner, providing
the largest single post-processing jump for every configuration (e.g.\ Ours rises
from $55.87$ to $61.98$). The intermediate steps are minor: min-normalisation
slightly \emph{lowers} the score before the Harris fusion penalty recovers it,
so the two roughly cancel.

The most striking observation is that the two final \textsc{Basic} steps --
the contrastive \textbf{projection} (Equation~\eqref{eq:basic_proj}) and
\textbf{query expansion} -- \emph{hurt} every configuration on SigLIP2: Ours
falls from its $61.98$ peak to $59.02$ and then $57.29$. These two steps were
designed and tuned for CLIP and do not transfer to the SigLIP2 embedding space.
The practical consequence is that the optimal pipeline on the best backbone
stops at contextualisation; the elaborate tail of the \textsc{Basic} stack can be
discarded.

\paragraph{Source of the centering mean.} A natural question
(Section~\ref{subsect:method_centering}) is whether the \emph{source} of the
mean matters. We find it does not: centering with the global database mean and
with a generic LAION-1M mean differ by at most $\sim\!0.55$ macro-mAP, and
usually by less. The choice of centering corpus is therefore not a material
hyperparameter, and we use the global database mean throughout.

\subsection{Caption Instruction Variants}\label{subsect:res_ablation_instr}

Table~\ref{tab:res_instr} compares the five caption-instruction variants of
Section~\ref{sect:method_caption} across three scoring modes on CLIP ViT-L/14:
caption alone ($\sc$ as a single-text query), caption $\times$ image (duplet
using $\sc$ in place of $\st$), and the full triplet (caption $\times$ text
$\times$ image, Equation~\eqref{eq:triplet}).

Three observations stand out.
\textbf{First}, caption alone consistently outperforms caption $\times$ image
for every instruction -- adding the image embedding hurts rather than helps.
This re-confirms the finding of Section~\ref{sect:res_main}: when the backbone
alignment is imperfect, multiplying with the image drags the query back towards
the unmodified instance appearance and undercuts the modification.
\textbf{Second}, caption alone already rivals the Text $\times$ Image duplet
($17.95$), showing that a well-formed target description, on its own, is nearly
as informative as combining the image and text signals.
\textbf{Third}, the triplet scores substantially outperform both caption-only
modes, confirming that the three branches are complementary: the caption supplies
the target-description signal that the modification text $\st$ cannot convey
alone, while the image branch anchors the instance identity.

The instruction ordering is consistent with design intent: \textsc{J}
(\emph{draft--refine}, $23.83$) and \textsc{G} (\emph{constraint-priority},
$23.51$) lead, while the rigid slot-fill template \textsc{I} trails. The spread
across instructions is modest ($\sim\!2.8$ points), and all five outperform the
duplet baseline ($17.95$). \texttt{avg-5} exceeds every single instruction and
is obtained without any selection on the evaluation set.

% SOURCE: runs/caption_backbone_instancepool/clip_l/summary.csv
%   caption = INSTR_*|caption ; product = INSTR_*|product ; triple = INSTR_*|triple
\begin{table}[h!]
\centering
\begin{tabular}{l l c c c}
\toprule
ID & Instruction & Caption & Cap$\times$Img & Triplet \\ \midrule
\textsc{J} & draft--refine           & 16.16 & 14.04 & \textbf{23.83} \\
\textsc{G} & constraint-priority     & 15.34 & 13.11 & 23.51 \\
\textsc{H} & anti-noise contract     & 14.51 & 12.76 & 23.51 \\
\textsc{I} & slot-fill template      & 16.26 & 15.89 & 23.12 \\
\textsc{F} & adaptive precision      & 16.08 & 15.14 & 22.57 \\ \midrule
           & \texttt{avg-5}          & 19.51 & 16.26 & \textbf{25.41} \\
\midrule
\multicolumn{2}{l}{Text $\times$ Image (\textsc{Basic} duplet)} & \multicolumn{3}{c}{17.95} \\
\bottomrule
\end{tabular}
\caption[Caption-instruction comparison across scoring modes (CLIP-L, full i-CIR).]{Macro-mAP
(\%) on the full i-CIR dataset, CLIP ViT-L/14 backbone. Caption: $\sc$ as a
text query; Cap$\times$Img: caption $\times$ image duplet; Triplet: full
caption $\times$ text $\times$ image product. Caption alone consistently
outperforms Cap$\times$Img for every instruction. The bottom row is the Text
$\times$ Image baseline for reference.\label{tab:res_instr}}
\end{table}

\subsection{Caption Augmentation: \texttt{avg-5} vs.\ Single Instructions}\label{subsect:res_aug}

Rather than commit to one prompt, the default caption embedding averages all
five instructions in embedding space (\texttt{avg-5},
Equation~\eqref{eq:avg5}). Table~\ref{tab:res_aug} shows this is a consistent
improvement: on \emph{every} backbone, \texttt{avg-5} exceeds both the best
single instruction and a selective average of the best four. The gain over the
best single instruction is small but reliable ($+0.9$ to $+2.2$ macro-mAP), and
crucially it is obtained \emph{without selecting} anything on the evaluation set
-- \texttt{avg-5} uses all five prompts unconditionally, whereas ``best
instruction'' and ``best-4'' require an oracle choice. Averaging the embeddings
keeps the signal that is consistent across instructions and attenuates the
idiosyncrasies of any single prompt, which is why we adopt it as the default.

% SOURCE: runs/caption_backbone_instancepool/<bk>/summary.csv (triple):
%   best single INSTR_*, AUG_AVG5, AUG_BEST4
\begin{table}[h!]
\centering
\begin{tabular}{l c c c}
\toprule
Backbone & best single & best-4 avg & \texttt{avg-5} \\ \midrule
CLIP-L   & 23.83 & 25.27 & \textbf{25.41} \\
CLIP-H   & 45.63 & 46.88 & \textbf{47.05} \\
SigLIP-L & 41.88 & 43.16 & \textbf{43.33} \\
SigLIP2  & 53.91 & 55.63 & \textbf{56.09} \\
\bottomrule
\end{tabular}
\caption[Caption augmentation: averaging vs.\ single instructions.]{Triplet
macro-mAP (\%) on the full i-CIR dataset. ``best single'' and ``best-4 avg''
require selecting instructions on the eval set; the unconditional \texttt{avg-5}
matches or beats both on every backbone.\label{tab:res_aug}}
\end{table}

% =====================================================================
\section{Backbone Sensitivity and the Performance Ceiling}\label{sect:res_backbones}
% =====================================================================

Table~\ref{tab:res_backbones} reports macro-mAP and micro-mAP for both the
\textsc{Basic} duplet and Ours (\texttt{avg-5} triplet) across all four
backbones. Two patterns emerge.

\textbf{The duplet ranking does not predict the triplet ranking.}
On the bare duplet, CLIP-H is the strongest backbone ($41.10$ mmAP), ahead of
SigLIP2 ($38.11$). Once the caption branch is added, SigLIP2 jumps to first
place ($56.09$) while CLIP-H's gain is far smaller ($47.05$). A backbone must
therefore be evaluated \emph{within} the full pipeline: a strong duplet encoder
is not necessarily the best partner for the caption.

\textbf{The triplet is uniformly better than the duplet on every backbone.}
The margin ranges from $+5.95$ (CLIP-H) to $+22.23$ (SigLIP-L). For SigLIP-L and
SigLIP2 the caption more than doubles the raw duplet score, confirming that these
encoders benefit the most from the additional textual modality.

The macro-mAP (mean-per-instance AP) and micro-mAP (mean-per-query AP) rankings
agree for Ours but diverge for the duplet on SigLIP-L ($21.10$ macro vs.\
$23.13$ micro): micro-mAP up-weights queries from instances with many queries,
giving a rosier picture for that backbone. Macro-mAP is the more conservative
and instance-fair metric and is used for all headline comparisons.

Recall@10 corroborates the pattern. On the duplet, CLIP-H leads with $83.5\%$
(meaning the correct instance appears in the top-10 for $83.5\%$ of queries);
after adding the caption, SigLIP2 reaches $92.2\%$ -- a gain of $+13.3$ points --
while CLIP-H gains only $+6.4$. The recall picture is consistent with the mmAP
ranking and further confirms that backbone choice must be made within the full
pipeline.

% SOURCE: runs/caption_backbone_instancepool/<bk>/summary.csv
%   BASIC = BASE_modtext|product ; Ours = AUG_AVG5|triple
%   Recall computed from topk_rankings.npy (200-deep rankings)
\begin{table}[h!]
\centering
\small
\begin{tabular}{l c c c c c c}
\toprule
 & \multicolumn{3}{c}{\textsc{Basic} (duplet)} & \multicolumn{3}{c}{Ours (\texttt{avg-5})} \\
\cmidrule(lr){2-4}\cmidrule(lr){5-7}
Backbone & mmAP & mAP & R@10 & mmAP & mAP & R@10 \\ \midrule
CLIP ViT-H/14       & \textbf{41.10} & 37.29 & 83.5 & 47.05 & 46.49 & 89.9 \\
SigLIP2 g/16-384    & 38.11 & 35.42 & 78.9 & \textbf{56.09} & \textbf{54.88} & \textbf{92.2} \\
SigLIP ViT-L/16-256 & 21.10 & 23.13 & 64.6 & 43.33 & 47.51 & 87.9 \\
CLIP ViT-L/14       & 17.95 & 21.85 & 65.6 & 25.41 & 30.80 & 72.8 \\
\bottomrule
\end{tabular}
\caption[Backbone comparison: \textsc{Basic} duplet vs.\ Ours (\texttt{avg-5}), full i-CIR.]{Macro-mAP
(mmAP), micro-mAP (mAP) and Recall@10 (\%) on the full i-CIR dataset, for the
\textsc{Basic} duplet and the Ours (\texttt{avg-5}) triplet. CLIP-H leads the
duplet on mmAP but SigLIP2 leads once the caption is added; recall@10 confirms
the same ordering.\label{tab:res_backbones}}
\end{table}

\paragraph{The performance ceiling.} Table~\ref{tab:res_ceiling} traces the
best result attainable as the pipeline is completed. The best bare-duplet
backbone (CLIP-H) reaches $41.10$; adding the caption lifts the best backbone to
SigLIP2 at $56.09$; and layering the post-processing that transfers to SigLIP2
(centering and contextualisation, Section~\ref{sect:res_centering}) raises the
high-water mark to $\mathbf{61.98}$ macro-mAP. The caption accounts for the
larger share of the journey from the duplet baseline to this ceiling ($+14.98$
out of the total $+20.88$).

\begin{table}[h!]
\centering
\begin{tabular}{l l c}
\toprule
Stage & Best configuration & mmAP \\ \midrule
Duplet baseline                & CLIP-H, img$\times$txt               & 41.10 \\
\quad $+$ target caption       & SigLIP2, Ours [\texttt{avg-5}]       & 56.09 \\
\quad $+$ post-processing      & SigLIP2, Ours $+$ center $+$ context & \textbf{61.98} \\
\bottomrule
\end{tabular}
\caption[Performance ceiling on the full i-CIR dataset.]{The best result
attainable at each stage of the pipeline. The caption ($+14.98$ mmAP) contributes
the larger share of the gain over the bare duplet, versus post-processing
($+5.89$).\label{tab:res_ceiling}}
\end{table}

% =====================================================================
\section{Qualitative Analysis}\label{sect:res_qual}
% =====================================================================

\subsection{Representative Successes}

% HIGHLIGHT: assemble Figure from real retrievals (top50_db_paths.npy +
% caption CSVs). Show 2-3 success cases: reference image q^v | modification
% text q^t | generated caption c | top-k retrieved (mark ground-truth instance).
Figure~\ref{fig:qualitative} shows representative queries where the triplet
succeeds and the duplet does not. In each, the generated caption makes the
composition explicit -- naming the instance and applying the requested
modification -- so that the retrieved images match both the instance identity and
the edit, whereas the duplet retrieval, driven by the fragmentary modification
text alone, returns visually plausible but semantically wrong candidates.

\begin{figure}[h!]
\centering
% \includegraphics[width=\textwidth]{figures/qualitative_success.png}
\caption[Qualitative successes.]{Example queries with the reference image, the
modification text, the generated target caption, and the top retrieved images
(ground-truth instance highlighted).\label{fig:qualitative}}
\end{figure}

\subsection{Failure Analysis}\label{subsect:res_failure}

% HIGHLIGHT: build the per-category breakdown from per_query_ap.npy joined to the
% query metadata (instance category), and pick 3-5 low-AP examples per mode.
Examining the lowest-AP queries reveals three recurring failure modes, which
mirror the caption-design trade-offs identified in
Section~\ref{sect:method_caption}.
\textbf{(i) Sparse captions for unrecognised instances.} When the MLLM cannot
identify the specific instance, it falls back to a generic description (e.g.\
collapsing a particular bridge to ``a bridge''), and the caption loses the
discriminative detail needed to separate the correct instance from look-alikes.
\textbf{(ii) Residual scene and aesthetic noise.} When the caption admits
background, lighting, or mood terms, the text query drifts toward visually
similar but semantically irrelevant images.
\textbf{(iii) Lost instance identity under constraint decomposition.} When the
modification is decomposed into atomic constraints, the caption occasionally
satisfies the edit while under-specifying the instance, retrieving the right
modification on the wrong object.

% HIGHLIGHT: insert (a) a per-category AP table and (b) a figure of 2-3 failure
% cases, one per mode, with the generated caption and the mis-ranked top-k.
A per-category breakdown (to be inserted) localises these modes to specific
instance categories, and the qualitative failure cases of
Figure~\ref{fig:failure} illustrate each. The analysis confirms that the
remaining errors are concentrated where caption \emph{quality}, not the retrieval
mechanism, is the bottleneck -- pointing to caption generation as the most
promising avenue for further gains.

\begin{figure}[h!]
\centering
% \includegraphics[width=\textwidth]{figures/qualitative_failure.png}
\caption[Qualitative failure cases.]{One example per failure mode: sparse
caption, residual scene noise, and lost instance identity, each with the
generated caption and the mis-ranked retrievals.\label{fig:failure}}
\end{figure}

% =====================================================================
\section{Summary}\label{sect:res_summary}
% =====================================================================

The experiments support the central claim of the thesis. The generated target
caption is the single most effective component of the pipeline: it improves every
backbone (Table~\ref{tab:res_main}), contributes a larger gain than the entire
\textsc{Basic} post-processing stack it replaces (Table~\ref{tab:res_ladder}),
and does so as a simple additive branch that needs no training. Embedding-space
averaging over the five instructions (\texttt{avg-5}) is a small but reliable,
selection-free improvement over any single prompt
(Table~\ref{tab:res_aug}). The best backbone within the full pipeline is SigLIP2,
not the strongest bare-duplet encoder (Section~\ref{sect:res_backbones}), and the
best result of the thesis -- $61.98$ macro-mAP -- combines the caption with the
two post-processing steps that transfer to SigLIP2, with the CLIP-specific
projection and query expansion discarded. The residual failures trace back to
caption quality rather than to the retrieval mechanism
(Section~\ref{subsect:res_failure}).

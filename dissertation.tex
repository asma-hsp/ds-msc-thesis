%!TEX TS-program = xelatex
%!TEX encoding = UTF-8 Unicode

% Dissertate.cls loads hyperref itself; passing `pdfa` here (before the class
% loads hyperref) is required so hyperref's PDF/A mode actually sticks --
% hyperref permanently locks the `pdfa` option after its own package-option
% processing, so setting it later via \hypersetup (as pdfx does when it finds
% hyperref already loaded) is silently ignored.
\PassOptionsToPackage{pdfa}{hyperref}
\documentclass{Dissertate}
\usepackage[a-2b]{pdfx}
\usepackage{titlesec}
\usepackage{fontspec}
\usepackage{amsbsy}
\usepackage{bm}
\usepackage[export]{adjustbox}
\usepackage{tikz}
\usepackage{float}   % [H] placement -- keeps large figures exactly where written
\usetikzlibrary{arrows.meta,shapes.geometric,positioning,calc,backgrounds}
\usepackage{colortbl}
\raggedbottom  % don't stretch text to fill the page when a big [H] figure is deferred

% ---- Notation macros (shared by all chapters) ------------------------
%  Aligned with the i-CIR paper: q^v image (visual) query, q^t text query,
%  X database, phi^v / phi^t visual / textual encoders.
\newcommand{\qv}{q^{v}}            % query (reference) image
\newcommand{\qt}{q^{t}}            % query (modification) text
\newcommand{\qc}{c}               % generated target caption
\newcommand{\DB}{\mathcal{X}}     % image database
\newcommand{\xdb}{x}              % a database image
\newcommand{\encv}{\phi^{v}}      % visual encoder
\newcommand{\enct}{\phi^{t}}      % textual encoder
\newcommand{\simop}{\mathrm{sim}} % cosine similarity
\newcommand{\clamp}{\mathrm{clamp}}
\newcommand{\sv}{s_{v}}           % image (visual) similarity score
\newcommand{\st}{s_{t}}           % text similarity score
\renewcommand{\sc}{s_{c}}         % caption similarity score (overrides old \sc font cmd)
% --- legacy aliases (kept so older drafts still compile) -------------
\newcommand{\Iq}{q^{v}}
\newcommand{\tq}{q^{t}}
\newcommand{\qcap}{c}
\newcommand{\db}{\mathcal{X}}
\newcommand{\enc}{\phi}
% ---------------------------------------------------------------------

\begin{document}

% the front matter
\input{frontmatter/personalize}
\frontmatter
\setstretch{\dnormalspacing}

% include each chapter...
\setcounter{chapter}{0}  % start chapter numbering at 1
\include{chapters/introduction}
\include{chapters/background}
\include{chapters/related_work}
\include{chapters/benchmark}
\include{chapters/methodology}
\include{chapters/results}
\include{chapters/conclusion}

% appendices
\appendix
\include{chapters/appendix_prompts}
\include{chapters/appendix_failure_analysis}
\include{chapters/appendix_inference_time}

\cleardoublepage
\phantomsection
\addcontentsline{toc}{chapter}{References}
\bibliography{references}

\cleardoublepage
\phantomsection
\addcontentsline{toc}{chapter}{Acknowledgments}
\acknowledgments

\end{document}
